import torch
import torch.nn as nn

from argparse import Namespace


def Transformer(nn.Module):
    def __init__(self, params: Namespace) -> None:
        super().__init__()


def Conformer(Transformer):
    def __init__(self, params: Namespace) -> None:
        super().__init__(params)